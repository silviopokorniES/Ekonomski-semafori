# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.ipynb_checkpoints/

# Project: local Excel inputs and generated outputs. data/README.md stays tracked.
data/*.xlsx
output/

# Local only, kept on disk but not published: the maintainer's notes and plan, the Claude Code rules,
# and the R parity fixtures (5 MB of raw Eurostat data and R output archives). The parity tests skip without them.
notes/
/CLAUDE.md
tests/fixtures/

# Editors and OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# R (only if the legacy scripts are ever run from a scratch copy)
.Rhistory
.RData
.Rproj.user/
.Renviron
